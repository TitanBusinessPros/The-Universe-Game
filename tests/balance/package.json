{
  "name": "after-earth-balance-simulation",
  "version": "1.0.0",
  "description": "Statistical AI-vs-AI balance simulation for the After Earth game (see ../README.md)",
  "private": true,
  "scripts": {
    "test": "node balance-simulation.js ../../index.html --runs=1 --turns=10 --frames=200 --difficulties=normal",
    "report": "node balance-simulation.js ../../index.html --runs=30 --turns=40 --frames=800 --difficulties=easy,normal,hard"
  },
  "dependencies": {
    "jsdom": "^29.1.1"
  }
}
